import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- ANA RENKLER (LACİVERT-MAVİ) ---
  static const Color primary = Color(0xFF7B9CDB); // Lacivert-Mavi
  static const Color primaryDark = Color(0xFF2D4A7A); // Derin Lacivert
  static const Color primaryLight = Color(0xFFB0C8EF); // Açık Mavi

  // İkincil ve Üçüncül
  static const Color secondary = Color(0xFF8E9BB3); // Gri-Lacivert
  static const Color secondaryDark = Color(0xFF3E4F6B); // Koyu Gri-Mavi
  static const Color tertiary = Color(0xFF7BAFCB); // Açık Çelik Mavi
  static const Color tertiaryDark = Color(0xFF2B5F7E); // Koyu Çelik

  // --- LIGHT THEME PALETİ ---
  static const Color lightBackground = Color(0xFFFFFBFE);
  static const Color lightSurface = Color(0xFFFFFBFE);
  static const Color lightSurfaceContainer = Color(0xFFF3EDF7);
  static const Color lightTextPrimary = Color(0xFF1C1B1F);
  static const Color lightTextSecondary = Color(0xFF49454F);

  // --- DARK THEME PALETİ ---
  static const Color darkBackground = Color(0xFF1C1B1F);
  static const Color darkSurface = Color(0xFF2B2930);
  static const Color darkSurfaceContainer = Color(0xFF36343B);
  static const Color darkTextPrimary = Color(0xFFE6E1E5);
  static const Color darkTextSecondary = Color(0xFFCAC4D0);

  // --- YARDIMCI RENKLER (SEMANTIC) ---
  static const Color error = Color(0xFFBA1A1A);
  static const Color success = Color(0xFF15803D);
  static const Color warning = Color(0xFFB45309);

  // --- YOKLAMA RENKLERİ ---
  static const Color greenPresent = Color(0xFF22C55E);
  static const Color redAbsent = Color(0xFFEF4444);
  static const Color amberExcused = Color(0xFFF59E0B);

  // Sosyal Medya
  static const Color googleRed = Color(0xFFDB4437);
}
